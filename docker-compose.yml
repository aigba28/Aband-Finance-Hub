version: "3.8"

services:
  finance-hub:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - finance-data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/finance.db
    restart: unless-stopped

volumes:
  finance-data:
